# Core
fintoc>=2.18.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
requests>=2.31.0

# Database
sqlalchemy>=2.0.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Data Processing
pandas>=2.0.0

# SAT / CFDI
zeep>=4.2.1

# Auth (uses stdlib hmac/hashlib for JWT + PBKDF2 - no external deps needed)
